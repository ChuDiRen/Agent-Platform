# Tasks

- [x] Create and verify OpenSpec and Comet lifecycle artifacts.
- [x] Add failing/targeted API tests for generation and template CRUD, then make them pass with backend routes.
- [x] Add required deepagents service integration with no rules fallback and no web-exposed shell/filesystem backend.
- [x] Register backend test-data routes and database model migration.
- [x] Add frontend typed API module for generation and templates.
- [x] Add generator page, route, and agent-hub navigation matching the reference UI.
- [x] Verify with `python -m pytest tests/api/test_test_data.py -q`.
- [x] Verify with `cd fronted && pnpm build:prod`.
